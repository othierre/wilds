import React from 'react';

const SpireIcon = ({ className, style }) => {
  return (
    <img 
      src="/spire.svg" 
      alt="Spire Icon" 
      className={className} style={style} />
  );
};

export default SpireIcon;